# Optional. Leave unset for local development and Docker so requests use the
# built-in /api proxy. Set this only when the API is hosted on another origin.
# VITE_API_BASE_URL=https://example.com/api
